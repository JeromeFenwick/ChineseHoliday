# 中国法定节假日日历订阅

这是一个可订阅的中国法定节假日日历，包含节假日和调休安排。

## 📅 订阅日历

在你的日历应用中添加以下订阅链接：

```
https://你的用户名.github.io/ChineseHoliday/chinese_holiday.ics
```

### 支持的日历应用

- **iOS/macOS 日历**：设置 → 日历 → 账户 → 添加账户 → 其他 → 添加已订阅的日历
- **Google Calendar**：左侧 "其他日历" → + → 通过 URL → 粘贴链接
- **Outlook**：日历 → 添加日历 → 从 Internet 订阅
- **Thunderbird**：右键日历列表 → 新建日历 → 网络位置

## 🚀 快速开始

### 1. 生成日历文件

```bash
python generate_calendar.py
```

### 2. 启用 GitHub Pages

1. 进入仓库的 **Settings** → **Pages**
2. 在 **Source** 中选择 `main` 或 `master` 分支
3. 点击 **Save**
4. 等待几分钟，你的日历文件就可以通过以下地址访问：
   ```
   https://你的用户名.github.io/ChineseHoliday/chinese_holiday.ics
   ```

### 3. 订阅日历

在你的日历应用中使用上述链接订阅即可。

## 📝 自定义节假日

编辑 `generate_calendar.py` 中的 `get_holidays_2025()` 函数来添加或修改节假日：

```python
holidays = [
    Holiday("节日名称", date(2025, 1, 1), date(2025, 1, 3), 
            description="节日描述"),
    # 添加更多节假日...
]
```

## 🤖 自动更新

项目包含 GitHub Actions 工作流，会在以下情况自动更新日历：

- 推送代码到主分支时
- 每月1号自动运行
- 手动触发（在 Actions 页面）

## 📦 文件说明

- `generate_calendar.py` - 日历生成脚本
- `chinese_holiday.ics` - 生成的日历文件
- `.github/workflows/generate-calendar.yml` - GitHub Actions 自动化配置

## 📄 许可证

MIT License

## 🙏 贡献

欢迎提交 Issue 和 Pull Request！
